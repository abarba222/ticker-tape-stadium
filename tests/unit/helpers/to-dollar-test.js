import { toDollar } from '../../../helpers/to-dollar';
import { module, test } from 'qunit';

module('Unit | Helper | to dollar');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = toDollar(42);
  assert.ok(result);
});
